# HunyuanImage-3.0 Dependencies
# Install with: pip install -r requirements_hunyuan.txt

# Core dependencies
einops==0.8.1
numpy==2.2.0
pillow>=11.0.0,<12.0
diffusers==0.35.2
safetensors==0.7.0
tokenizers==0.22.0
transformers[accelerate,tiktoken]==4.57.1
huggingface_hub[cli]
loguru>=0.7.3

# Gradio UI
gradio==5.23.1

# PyTorch with CUDA 12.8 (install separately)
# pip install torch==2.8.0 torchvision==0.23.0 torchaudio==2.8.0 --index-url https://download.pytorch.org/whl/cu128

# Optional: FlashInfer for ~3x faster MoE inference
# pip install flashinfer-python==0.5.0
